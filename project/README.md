# Streamlit 기반 중고차 데이터 분석 서비스

중고차 매물 데이터(`data/used_cars.csv`)를 활용해 브랜드, 연식, 주행거리, 연료 유형, 사고 이력에 따른 가격 분포를 탐색하는 Streamlit 데이터 분석 프로젝트입니다.

## 주요 기능

- 전체 매물 수, 평균 가격, 평균 주행거리, 평균 연식 요약
- 브랜드, 연식, 가격, 주행거리, 연료 유형, 사고 이력 필터링
- 전체/일반 가격대/가격 구간별 분포, 브랜드별 평균 가격, 연식별 평균 가격, 주행거리-가격 관계 시각화
- 가격 상위 1% 초고가 매물을 따로 보여주는 비싼차 모음집
- 학습된 머신러닝 모델을 이용한 중고차 가격 예측
- 차량 정보를 보고 실제 가격을 맞히는 업/다운 가격 예측 게임
- 필터링 결과에 대한 간단한 데이터 인사이트 제공
- 조건에 맞는 중고차 목록 테이블 제공

## 프로젝트 구조

```text
project/
├── app.py
├── data/
│   └── used_cars.csv
├── docs/
│   ├── presentation_summary.md
│   └── streamlit_project_plan.md
├── pages/
│   ├── 1_EDA.py
│   ├── 2_시각화.py
│   ├── 3_모델_서비스.py
│   └── 4_가격예측게임.py
├── src/
│   ├── __init__.py
│   ├── charts.py
│   ├── data_loader.py
│   ├── insights.py
│   └── model.py
├── requirements.txt
├── README.md
├── 기획서.md
├── 보고서.md
└── .gitignore
```

## 실행 방법

1. 가상환경 생성 및 활성화

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
```

2. 의존성 설치

```powershell
pip install -r requirements.txt
```

3. Streamlit 앱 실행

```powershell
streamlit run app.py
```

## 데이터 설명

| 컬럼명 | 설명 |
|---|---|
| `brand` | 차량 브랜드 |
| `model` | 차량 모델 |
| `model_year` | 차량 연식 |
| `milage` | 주행거리 |
| `fuel_type` | 연료 유형 |
| `engine` | 엔진 정보 |
| `transmission` | 변속기 정보 |
| `ext_col` | 외장 색상 |
| `int_col` | 내장 색상 |
| `accident` | 사고 또는 손상 이력 |
| `clean_title` | 클린 타이틀 여부 |
| `price` | 차량 가격 |

## 실행 화면 구성

앱은 상단 탭과 왼쪽 사이드바 필터를 중심으로 구성됩니다.

- `개요`: 전체 데이터의 매물 수, 평균 가격, 평균 주행거리, 평균 연식을 보여주고 전체 가격 분포, 일반 가격대 세부 분포, 가격 구간별 매물 수를 시각화합니다.
- `브랜드 분석`: 브랜드별 평균 가격과 매물 수를 비교하여 어떤 브랜드가 높은 가격대를 형성하는지 확인합니다.
- `연식/주행거리`: 연식별 평균 가격 변화와 주행거리-가격 산점도를 통해 차량 상태와 가격의 관계를 분석합니다.
- `가격 예측`: 모델 학습에서 제외한 초고가 매물을 비싼차 모음집으로 따로 보여주고, 사용자가 브랜드, 모델, 연식, 주행거리, 연료 유형, 변속기, 사고 이력 등을 입력하면 학습된 모델이 예상 가격을 출력합니다.
- `가격 예측 게임`: 실제 차량 정보를 보고 가격을 입력하면 UP/DOWN 힌트와 정답 근접 여부를 알려주는 게임형 서비스입니다.
- `데이터`: 필터 조건에 맞는 실제 중고차 목록을 표 형태로 확인합니다.

## 가격 예측 모델

앱 실행 시 `data/used_cars.csv`를 기반으로 가격 예측 모델을 학습합니다.

- 사용 모델: RandomForestRegressor
- 입력 변수: 브랜드, 모델, 연식, 주행거리, 연료 유형, 변속기, 사고 이력, 클린 타이틀 여부
- 예측 대상: 차량 가격
- 초고가 이상치 영향을 줄이기 위해 가격 상위 1% 매물은 모델 학습에서 제외
- 제외된 초고가 매물은 `비싼차 모음집`에서 브랜드, 모델명, 연식, 주행거리, 가격을 별도로 확인 가능
- Streamlit의 `가격 예측` 탭에서 사용자가 조건을 입력하면 예상 가격을 확인할 수 있음

## 발표 핵심 요약

- 데이터 전처리: 가격과 주행거리 문자열에서 기호와 단위를 제거하고 숫자형으로 변환했으며, 결측값은 `Unknown` 처리 또는 분석 대상에서 제외했습니다.
- 주요 분석 결과: 전체 가격 분포와 일반 가격대 세부 분포를 나누어 확인하고, 브랜드, 연식, 주행거리, 사고 이력에 따른 가격 차이를 시각화로 확인할 수 있습니다.
- 예측 서비스: RandomForest 기반 모델을 학습시켜 사용자가 입력한 차량 조건에 대한 예상 가격을 제공합니다.
- 게임 서비스: 실제 매물 데이터를 활용해 사용자가 가격을 추측하고 UP/DOWN 피드백을 받는 인터랙티브 기능을 제공합니다.
- 모델 성능: 가격 상위 1% 초고가 이상치를 제외한 뒤 평균 절대 오차 약 `$12,141`, R² 약 `0.609` 수준으로 개선되었습니다.
